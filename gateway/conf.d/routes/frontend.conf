# Next.js frontend routes (all assets and pages fully contained within /web)
location /web {
    proxy_pass http://frontend:3000;
    include /etc/nginx/includes/proxy-params.conf;
    include /etc/nginx/includes/websocket-params.conf;
}
